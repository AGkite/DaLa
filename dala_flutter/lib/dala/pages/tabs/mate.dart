import 'package:flutter/material.dart';

class MatePage extends StatelessWidget {
  const MatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: const Text("搭子页面"),
    );
  }
}
